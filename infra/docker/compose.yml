services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: wellfin
      POSTGRES_USER: wellfin
      POSTGRES_PASSWORD: wellfin
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

  api:
    build:
      context: ../..
      dockerfile: infra/docker/api.Dockerfile
    environment:
      PORT: 8080
      DATABASE_URL: postgres://wellfin:wellfin@db:5432/wellfin
    ports:
      - "8080:8080"
    depends_on:
      - db

volumes:
  db-data:


